# DOCs/PDR — 需求文档(Product / Design Requirements)

本文件夹存放 `pre-symphony` 的需求与设计陈述,先于实现。命名沿用 `YYYYMMDD-<名称>.md`。

| 文件 | 内容 |
|---|---|
| [20260528-pre-symphony-requirements.md](20260528-pre-symphony-requirements.md) | 项目需求总纲:背景、范围、PLAN DAG 模型、功能/非功能需求、架构、linear-cli 能力映射、开放问题 |

> 命名说明:用户指定文件夹名为 `PDR`(沿用其指定;业界常用 `PRD = Product Requirements Document`,如需统一可改名)。
